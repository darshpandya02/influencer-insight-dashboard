export async function queryRAGEngine(query: string): Promise<string> {
  // Mock implementation. Replace with actual call to Quant vector store + LLM.
  return `Insight for query: "${query}" — Trending topics include health tips, morning routines, and gym hacks.`;
}